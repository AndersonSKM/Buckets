coverage:
  status:
    project:
      default:
        target: 100
    patch:
      default:
        target: 100
  range: 95..100
  round: down
  precision: 2
    fixes:
    - "/api/::api/"
    - "/client/::client/"

comment: off
